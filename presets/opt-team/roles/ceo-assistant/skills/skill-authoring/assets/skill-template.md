# SKILL.md 骨架模板

> 复制这份骨架到 `skills/_pending/<name>/SKILL.md`，逐段填。
> 每段后面的方括号是填写要求，填完删掉。判据见 `_shared/SKILL-SPEC.md`。

```markdown
---
name: <目录同名，小写字母数字连字符，1-64 字符>
description: <做什么 + 什么时候用（塞真实触发语）+ 什么时候不用（指向哪条技能）。≤1024 字符，写成一行。>
license: Proprietary. 内部使用。
compatibility: 面向 OpenClaw 工作区技能；<需要哪些运行时能力，缺了怎么降级>
metadata:
  author: OPT-Team
  version: "1.0"
  role: <哪个或哪几个角色用>
  updated: "<YYYY-MM-DD>"
allowed-tools: <只写真正需要的。诊断类不写 Write 到别人工作区，调研类不写 exec>
---

# <中文名> · <name>

> <一句话定位。说清它替搭档承担了什么，不是它有多强。>

## 硬约束

<3-6 条，动词开头，读到就生效的红线。至少一条是"未满足本步退出条件，不得进入下一步"级别的硬门。>

## 权限与数据边界

| 项 | 边界 |
|----|------|
| 读 | |
| 写 | <永远写明：不写入任何其他 Agent 的工作区> |
| 外发 | <有没有对外动作。有就写清需要谁授权> |
| 敏感数据 | <什么绝对不能进搜索串 / 不能进对外稿> |
| 升权 | <需不需要 exec / sudo / 装包> |

## 何时使用 / 何时不用

| 场景 | 用不用 |
|------|--------|
| <真实说法> | ✅ 用 |
| <真实说法> | ❌ → `<别的技能>` |

**升级信号**：<发现问题比本技能大时，转给谁>
**降级信号**：<发现问题比本技能小时，转给谁>

## 输入输出契约

**输入**：
**输出**：<落到哪个文件，含哪些字段>
**上游**（谁的输出能喂给我）：
**下游**（我的输出能喂给谁）：
**串联示例**：`A` → 本技能 → `B`

## 流程

### Step 1 · <动词开头>

**目标**：
**动作**：
**标准话术**：> "<真的会说出口的一句话>"
**退出条件**：<可二值判定。写完这句：未满足本步退出条件，不得进入下一步。>
**常见失败**：

### Step 2 · ...

## 自检闸门

- [ ] <条目>　→ 不过回 Step X

## 量化验收指标

| 指标 | 门槛 |
|------|------|
| <可数的> | <数字> |

<全表不许出现形容词。>

## 常见失败模式

| 失败 | 表现 | 怎么避免 |
|------|------|---------|

## 降级与失败路径

<做不到时怎么办。三条底线：不编、不糊弄、如实说清哪块没做到。>

## 执行留痕与记忆写回

**留痕**：<留什么，谁能凭它复现这次执行>
**写回 `memory/YYYY-MM-DD.md`**：<1-4 条>
**沉淀进 `MEMORY.md`**：<最有复利的那一条>
```

## 填写顺序建议

先写「何时使用 / 何时不用」，再写「流程」，最后回头写 `description`。
反过来写的结果是 description 像简介，边界像补丁。
