# references/ — 按需读取的参考资料

## oscar-data-pack.md（需自行放入）

方法论框架库，约 76KB。**禁止全量读取**——会瞬间吃掉上下文预算。按场景 Grep 精准定位：

| 场景 | 搜索关键词 |
|------|-----------|
| 机会预判 | `机会预判` |
| 商业可行性 | `一堂五步法` |
| 渠道选择 | `渠道探索` |
| ROI 评估 | `科学决策ROI` |

用法示例：

```bash
grep -n -A 40 "一堂五步法" references/oscar-data-pack.md
```

## 可信度分级

不放在这里。团队共用一份，在工作区根目录 `reference/credibility-standards.md`。
各技能引用它即可，**不要各写一份**——标准分叉是交叉验证失效的开始。
