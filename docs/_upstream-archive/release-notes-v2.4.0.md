# OpenClaw Chat Gateway v2.4.0

- Keep process output separate from the final answer, matching OpenClaw tool-flow semantics.
- Route ClawUI image generation only from explicit agent `.md` defaults, avoiding accidental image-model calls while restoring drawing agents.
- Update image model settings copy to reflect the `.md`-driven behavior.
